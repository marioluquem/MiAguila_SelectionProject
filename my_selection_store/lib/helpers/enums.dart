//Enum para controlar cuando esta cargando cada sección de productos del App
enum EnumProductsLoadingState {
  loadingFeatured,
  loadingScroll,
  loadingCart,
}

//Enum para controlar los tipos de conexión del dispositivo
enum InternetConnectionType { mobile, wifi, disconnected }
